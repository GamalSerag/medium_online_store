name: Deploy Production

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Deploy Production workflow is disabled. Use Deploy to VPS for deployments."
